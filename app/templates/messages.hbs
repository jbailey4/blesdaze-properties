<div class="ui grid container contact-form-container">
  <div class="row">
    <div class="sixteen wide device twelve wide tablet ten wide computer column">
      <p>Please feel free to contact us with any questions about a home you like, schedule a viewing, or any other general questions you may have using the form below. You can call us too at <a href="tel:17045265278">704-526-5278</a>.</p>
    </div>
  </div>

  <div class="row">
    <div class="sixteen wide device twelve wide tablet eight wide computer column">
      <form id="contact-form" class="ui large form {{if errorMessage 'error'}} {{if isCompleted 'success'}}" {{action "sendMessage" on="submit"}}>
        <div class="ui success message">
          <div class="header">Message Sent</div>
          <p>Thanks for the message, we will get back to you soon as possible.</p>
        </div>
        <div class="ui error message">
          <div class="header">We had some issues</div>
          <p>{{errorMessage}}</p>
        </div>

        <div class="required field">
          <label for="contact-email">Email</label>
          <input type="email" id="contact-email" placeholder="you@example.com" onchange={{action (mut model.email) value="target.value"}}>
        </div>

        <div class="field">
          <label for="contact-phone">Phone</label>
          <input type="text" id="contact-phone" onchange={{action (mut model.phoneNumber) value="target.value"}}>
        </div>

        <div class="required field">
          <label for="contact-message">Message</label>
          <textarea id="contact-message" rows="4" onchange={{action (mut model.message) value="target.value"}}></textarea>
        </div>

        <button type="submit" class="ui button" id="contact-submit">Submit</button>
      </form>
    </div>
  </div>
</div>
