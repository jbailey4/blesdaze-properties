import whichTransition from 'blesdaze-properties/utils/which-transition';
import { module, test } from 'qunit';

module('Unit | Utility | which transition');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = whichTransition();
  assert.ok(result);
});
